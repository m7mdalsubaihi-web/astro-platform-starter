{
  "name": "hamodeh-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "http-server public -p 4321",
    "start": "http-server public -p 4321",
    "build": "rm -rf dist && mkdir -p dist && cp -R public/. dist/",
    "preview": "http-server dist -p 4321"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
